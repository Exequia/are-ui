describe('Auth Actions', () => {
  /* it('should begin a new search', () => {
    const payload: CopyrightSearch = {
      term: '---',
      namespace: '___'
    };
    const action = new fromActions.DoSearch(payload);
    expect({ ...action }).toEqual({
      type: fromActions.DO_SEARCH,
      payload
    });
  });
  it('should create a Search Fail action', () => {
    const payload = 'error';
    const action = new fromActions.DoSearchFail(payload);
    expect({ ...action }).toEqual({
      type: fromActions.DO_SEARCH_FAIL,
      payload
    });
  });
  it('should create a Search Success action', () => {
    const payload: CopyrightSearchResult = {
      items: null,
      total: 10,
      tags: ['a', 'b']
    };
    const action = new fromActions.DoSearchSuccess(payload);
    expect({
      ...action
    }).toEqual({
      type: fromActions.DO_SEARCH_SUCCESS,
      payload
    });
  });
  it('should create a Search Results Toggle Visibility action', () => {
    const action = new fromActions.ToggleSearchResultsVisibility(true);
    expect({
      ...action
    }).toEqual({
      type: fromActions.TOGGLE_SEARCH_RESULTS_VISIBILITY,
      payload: true
    });
  });
  it('should create a Search Select Search Item action', () => {
    const payload = '----------';
    const action = new fromActions.SelectSearchItem(payload);
    expect({
      ...action
    }).toEqual({
      type: fromActions.SELECT_SEARCH_ITEM,
      payload
    });
  });*/
});
